# 开发日志 — 2026年9月1日

## 概述

政策分析模块完整落地：多标签数据模型 + LLM 批量标注（344 条）+ 手动标签编辑 + 5 个学术维度可视化（按设计草案）

---

## 一、数据模型: Policy 加 tags 多标签字段

### 改动
- `prisma/schema.prisma`: Policy 新增 `tags String @default("")`（逗号分隔 A-L，如 "A,B,K"）
- `type` 保留为主类型（= 主标签），与现有 API/UI 完全兼容
- db push 无损迁移（迁移前已备份 `prisma/dev.db.bak-20260901`，344 条数据完整）

### 发现
- 数据库实际政策数为 **344 条**（POLICIES.md 记录的 192 已过时——7 月 10 日爬虫 v5 后又跑过）

---

## 二、LLM 批量标注（DeepSeek）

### 脚本: `scripts/tag-policies.ts`
- 逐条调用 DeepSeek（jsonMode），300ms 限速 + 3 次指数退避重试
- `tags=""` 过滤实现断点续跑；`--dry-run`/`--limit`/`--force`/`--tags-only` 参数
- 垃圾标题预判（分页/导航/超短文本）不浪费 LLM 调用
- 无关政策自动标 `M-其他`（分析 API 过滤）
- 内置 loadEnv（npx tsx 不加载 .env）

### 结果（详见 POLICY-TAGS-REPORT-2026-09-01.md）
- 处理 344 条：成功 279，无关/垃圾 65（标 M-其他）
- **分类失衡显著纠正**: A-治理体系 49.1% → 27.9%；C/E/L/I/J 从极低/0 提升到合理水平
- 标注中发现约 30+ 条爬虫脏数据（招聘公告/分页文本/面包屑导航）

---

## 三、手动标签编辑（用户要求）

- `PATCH /api/policies/[id]`: tags 数组规范化（A-L 去重排序、上限 4），同步 type = 主标签
- PolicyModal 新增编辑面板：当前标签可删 + 12 类可加 + 保存/取消
- 保存后列表卡片与弹窗即时同步

---

## 四、政策分析 5 维度

### API: `/api/policies/analysis`（一次返回全部视图）
- 全量内存聚合（344 条规模）；tags 空 fallback type；M-其他过滤
- 地图数据排除"全国"（nationalCount 单独返回）
- 联合发文检测：department 字段 + 标题多机构提取（如"省教育厅 省财政厅"）

### 前端: StatsPanel 子标签「院校统计 / 政策分析」

| 维度 | 组件 | 实现 |
|------|------|------|
| 演进趋势 | TrendChart | 年×类型堆叠面积图（多标签口径） |
| 省份活跃度 | ProvinceActivity + PolicyChinaHeatmap | TOP10 条形图 + 中国地图热力 |
| 主题共现 | CooccurrenceGraph | 共现矩阵 / 力导向网络图双视图 |
| 发文主体 | DepartmentAnalysis | 部门饼图 + 联合发文列表（检出 16 份） |
| 密集度时序 | CalendarHeatmap | 月×年热力图 + 类型过滤 + 月度总量 |

---

## 五、工程重构

| 文件 | 操作 | 说明 |
|------|:--:|------|
| `src/lib/policy-types.ts` | **新建** | 12 类共享常量（LABELS/CLASSES/COLORS/工具函数），PolicyPanel 改用 |
| `src/lib/use-china-geo.ts` | **新建** | 地图加载共享 hook，模块级 promise 缓存防重复 fetch |
| `ChinaMap.tsx` | 重构 | 改用 hook（仅加载部分，onEvents 未动），失败态加重试 |
| `scripts/e2e-policy-analysis.ts` | **新建** | 12 项 E2E 验证，全部 PASS |

---

## 六、验证结果

- `npx tsc --noEmit` 通过
- Playwright E2E 12 项全部通过、0 pageerror：
  - 首页地图回归 ✅ 统计后台子标签 ✅ 5 维度图表 ✅ 矩阵/网络图切换 ✅ 类型过滤 ✅
  - 院校统计回归 ✅ 地图点省份→抽屉→遮罩关闭 ✅ 标签编辑保存 ✅
- API 数据抽查：total=344、province 广东 70/全国 23、cooccur 12 节点 41 边、jointCount=16

---

## 七、数据清洗（下午操作，用户指令）

### 7.1 脏数据分析（64 条 M-其他逐条审查）

| 类别 | 数量 | 处理 |
|------|:--:|------|
| 爬虫垃圾（分页/面包屑/菜单/栏目名） | 9 | 删除 |
| 高校招聘公告（博士招聘/拟聘用公示） | 10 | 删除 |
| 非职教活动（艺术节/奖学金/诵写讲/普通话/足球/创新大赛等） | 20+ | 删除 |
| 教师资格认定/名单公示/课题申报 | 8 | 删除 |
| 高校章程核准书 | 5 | 改标 A-治理体系 |
| 实习监督电话公告 | 2 | 改标 F-质量评价 |
| 职教技能大赛通知 | 3 | 改标 C-人才培养 |
| 自考+技工联合发文 | 1 | 改标 A-治理体系 |

**结果: 删除 60 条（首轮 53 + 全库扫描补 7），改标 11 条，政策总数 344 → 284，M-其他归零。**

### 7.2 爬虫防脏数据规则（写入 crawl-policies-real.ts）

- 新增 `isJunkTitle()`: 分页导航/面包屑/筛选菜单/栏目名/超短标题(<6字) 直接丢弃
- `EXCLUDE` 扩展: 公开招聘/拟聘用/教师资格认定/艺术节/奖学金/诵写讲/普通话/校园足球/创新大赛/终身学习/研究生英语/国防教育/法治作品/教育科学规划/实验室安全检查/获奖名单/名单公示/优秀毕业生
- 统计输出加「跳过(垃圾)」计数

### 7.3 教训: E2E 测试污染数据

- 第一版 e2e-policy-analysis.ts 步骤 11（标签编辑）直接保存，把 3 条脏数据改标（E-师资队伍），导致其逃过首轮清洗
- 修复: E2E 改为「取消不写库」；清洗脚本加 `--full` 全库扫描模式兜底

## 八、政策增删查改 CRUD

### API

| 方法 | 路由 | 说明 |
|------|------|------|
| POST | /api/policies | 手动新增（title/province/publishDate 必填校验，标签规范化） |
| PATCH | /api/policies/[id] | 全字段编辑（标题/省份/日期/部门/摘要/URL + 标签同步主类型） |
| DELETE | /api/policies/[id] | 删除 |
| GET | /api/policies | 已有（查询） |

### UI

- 政策库头部「＋ 新增政策」按钮 → PolicyFormModal（新增表单: 标题/省份/日期/部门/摘要/链接/12类标签点选）
- 政策详情弹窗新增「📝 编辑信息」（全字段表单）与「🗑 删除」（confirm 确认）按钮
- 原有「✏️ 编辑标签」快速入口保留
- 保存/删除后列表与总数即时同步

### 验证

- `scripts/e2e-policy-crud.ts`: 新增→编辑信息→编辑标签→删除→API 校验，全部 PASS，0 pageerror

## 九、测试环境部署

| 步骤 | 结果 |
|------|------|
| git push（6123c4e → 82f4d1b，14 个 commit） | ✅ |
| 服务器 git pull + 旧 db 备份（dev.db.bak-before-20260901） | ✅ |
| 上传清洗后数据库（284 条） | ✅ |
| `npx prisma generate`（修复 Client 未再生导致的 build 失败） | ✅ |
| PORT=3002 build + pm2 restart zhijiao-test | ✅ |
| 验证: http://47.107.31.231:3002 返回 200，分析 API total=284 | ✅ |

**生产环境（3001）未部署**（按用户指令仅部署测试环境；生产按 CLAUDE.md 每日一次节奏，17:00 后部署）。

## 十、Bug 修复: 类型筛选 chip 重复渲染（晚间，用户反馈）

### 现象
- 政策库页点击部分类型标签后列表显示"暂无政策数据"
- 搜索框在部分环境未渲染（本地 3000 残留 dev server 编译缓存不一致所致）

### 根因
- `policy-types.ts` 给 TYPE_LABELS 增加单字母键（"A"→"治理体系"）后，
  PolicyPanel 筛选 chip 用 `Object.entries(TYPE_LABELS)` 迭代出 **24 个键**（12 单字母 + 12 全名重复）
- 点击单字母键 chip 时 API `where.type = "A"` 匹配不到全名格式 "A-治理体系" 的记录 → 空列表

### 修复（commit 7faee42）
- 筛选 chip 改用 `TYPE_ORDER` 生成全名键（只渲染 12 个 chip）
- API type 参数兼容单字母格式（自动转全名查询）
- 清理本地 3000/3001 残留 dev server 进程

### 验证
- E2E 两个套件重跑 PASS；测试环境验证: 全名与单字母筛选均返回 15 条产教融合政策
- 已部署测试环境 3002

### 教训
- 共享常量加键时必须扫描所有 `Object.entries()` 迭代点（grep 检查）
- 本地起 dev server 前先确认端口无残留进程（netstat）

## 十一、待办（遗留）

- [ ] 生产环境 3001 待部署（用户确认后）
- [ ] 新增政策时未自动填 sourceOrg（爬虫数据有），后续可补
- [ ] 后续可用 LLM 对 summary 做精炼、或对标注做抽样复核

---

## 提交记录

```
82f4d1b fix: 全库脏数据扫描 + E2E 测试防写库
435c862 test: 政策 CRUD E2E 验证脚本
b3cd65e feat: 政策增删查改 CRUD
7f68bb8 fix: 爬虫防脏数据规则（清洗经验沉淀）
113b889 chore: 脏数据清洗脚本 + 执行（删 53 改标 11）
a0cf844 test: 政策分析模块 E2E 验证脚本
9fc8ee9 feat: 政策分析 5 维度组件 + StatsPanel 子标签
e27418a chore: LLM 标注 344 条政策完成 + 标注报告
1683e68 refactor: 地图加载提取 use-china-geo
9fdd210 feat: 政策分析聚合 API（5 视图）
dbb9a30 feat: 政策标签手动编辑
da89c52 feat: LLM 批量标注脚本 tag-policies
dfbe816 feat: Policy 加 tags 多标签字段
b78a498 refactor: 提取政策类型共享常量 policy-types
```
